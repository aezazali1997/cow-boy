import { createUseStyles } from "vue-jss";
const styles = createUseStyles({
  container: {
    position: "relative",
  },
  preOrder: {
    position: "absolute",
    bottom: 0,
    left: 0,
    right: 0,
  },
});
export default styles;
